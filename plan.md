1. **Analyze waveform.tsx `onMouseMove` handler:** In `src/components/audio/waveform.tsx`, the `handleMouseMove` function performs geometry calculations (`getBoundingClientRect()`) and state updates (`setHoveredHighlight`) without throttling. This can cause layout thrashing and main-thread blocking when high-frequency `mousemove` events are triggered.
2. **Implement `requestAnimationFrame` for `handleMouseMove`:** We will wrap the inner logic of `handleMouseMove` using `requestAnimationFrame`. We'll use a `useRef` to track the pending animation frame and ensure we cancel it if a new event comes in or the component unmounts. This explicitly complies with the memory rule regarding high-frequency UI events.
3. **Complete pre-commit steps:** Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
4. **Submit the PR:** Use the `submit` tool to submit the changes with a properly formatted PR body including the required `Bolt` sections (`### 💡 What`, `### 🎯 Why`, `### 📊 Impact`, `### 🔬 Measurement`, `### 🧪 Testing`, and `### 📝 Notes`).
